# 第一阶段实施方案（左右臂合并，V2 按用户最新指示修订）

> 日期：2026-09-10
> 范围：仅 `copy\LEFTNOW·`（合并后改名 `F4Arm`）。根目录、`copy\RIGHTNOW`、用户手建的副本一律不动。
> **用户最新指示（本次方案依据）：**
> 1. 左右臂通信协议的反向交叉是**有意实现**的 → 一律不回改，按名字区分：`LEFTNOW` = 左臂烧录代码，`RIGHTNOW` = 右臂烧录代码。
> 2. **死代码、未用模块第一阶段不清理**（后续要 debug），全部保留。
> 3. 不做任何 git 操作。
> 4. 阶段二之后的内容等第一阶段完成后再讨论。

---

## 0. 本阶段目标（唯一）

把左右臂两套工程合并为**一套源码 + 两个固件**，且**编译产物行为与现状完全一致**（左侧固件 = 现在 LEFTNOW·，右侧固件 = 现在 RIGHTNOW，含信号交叉等一切既有特性）。

**明确不做：** 死代码清理、未用模块删除、协议修改、行为"修正"、git 操作、任何右侧工程行为变更。

## 1. 前置确认（开工前仍在等您答复）

| # | 问题 | 影响 |
|---|---|---|
| 1 | 机器左边板子烧 `LEFTNOW`、右边板子烧 `RIGHTNOW` —— 已确认 ✓（您已按名字区分，无需再答） | 宏分配已定 |
| 2 | 两块副板是否同一物理 CAN1？ | 仅影响阶段四，本阶段不处理也不影响合并 |
| 3 | 合并后目录名 `F4Arm`？ | 本阶段第 2 步 |

> 协议交叉、信号组、FINISH 回执 **全部保持现状**，不再列入确认。

## 2. 步骤明细

### 步骤 1：改名
- `copy\LEFTNOW·` → `copy\F4Arm`
- uvprojx/.mxproject 内全为相对路径，改名安全（已核验）

### 步骤 2：新增 `F4Arm\Config\arm_config.h`

左右差异源，**逻辑与旧清单相同，但信号组按"名字"分配**（左板响应右臂码 0x05/06/08、右板响应左臂码 0x03/04/07 保持原样）：

```c
#ifndef ARM_CONFIG_H
#define ARM_CONFIG_H
#include <stdint.h>
#include <stdbool.h>

/* Keil Target Define：f4_show_LEFT → ARM_LEFT ；f4_show_RIGHT → ARM_RIGHT */
#if defined(ARM_LEFT)
  #define ARM_SOLENOID_CHANNEL   1U      /* 左臂：solenoid_init(1)  原 LEFTNOW· */
  #define ARM_DJI_INDEX          0U      /* 左臂：&DJmotor[0]       原 LEFTNOW· */
  #define ARM_PICK_SIGNAL        0x05U   /* 左臂响应组（原 left 工程启用 right handler） */
  #define ARM_PLACE_SIGNAL       0x06U
  #define ARM_RELEASE_SIGNAL     0x08U
#elif defined(ARM_RIGHT)
  #define ARM_SOLENOID_CHANNEL   2U      /* 右臂：solenoid_init(2U) 原 RIGHTNOW */
  #define ARM_DJI_INDEX          1U      /* 右臂：&DJmotor[1]       原 RIGHTNOW */
  #define ARM_PICK_SIGNAL        0x03U   /* 右臂响应组（原 right 工程启用 left handler） */
  #define ARM_PLACE_SIGNAL       0x04U
  #define ARM_RELEASE_SIGNAL     0x07U
#else
  #error "Define ARM_LEFT or ARM_RIGHT"
#endif

#define ARM_ZDRIVE_INDEX         0U
typedef struct { float dji_deg; float zdrive_deg; } ArmPose_t;
extern const ArmPose_t ARM_POSES_LEFT[7];
extern const ArmPose_t ARM_POSES_RIGHT[7];
#endif
```

### 步骤 3：新增 `F4Arm\Config\arm_poses.c`

左右两套 7 姿态角，**原样照搬**两边 `BlockArm.c` 实测值（左臂 LEFTNOW· 值：-12/118、-21/101、20/45、-20/90、45/50、85/44、20/40；右臂 RIGHTNOW 值：12/116、28/105、-20/45、20/95、-40/50、-84/44、-25/40）。

### 步骤 4：修改 `Core/Src/main.c`（2 处）

| 位置 | 改前 | 改后 |
|---|---|---|
| Includes | — | 加 `#include "arm_config.h"` |
| 第 118 行 | `solenoid_init(1);` | `solenoid_init(ARM_SOLENOID_CHANNEL);` |
| 第 127 行 | `BlockArm_Init(&DJmotor[0], &Zmotor[BLOCK_ARM_ZDRIVE_INDEX]);` | `BlockArm_Init(&DJmotor[ARM_DJI_INDEX], &Zmotor[ARM_ZDRIVE_INDEX]);` |

> 行为：左 Target 编译出 1/0，右 Target 编译出 2/1，与现状完全一致。

### 步骤 5：修改 `Mechanism/Src/BlockArm.c`（姿态查表，7 个 case + 头部）

- 头部加 `#include "arm_config.h"`；新增静态指针 `static const ArmPose_t *ARM_POSES = NULL;`
- `BlockArm_Init()` 内按编译宏赋值：
  ```c
  #if defined(ARM_LEFT)
      ARM_POSES = ARM_POSES_LEFT;
  #elif defined(ARM_RIGHT)
      ARM_POSES = ARM_POSES_RIGHT;
  #endif
  ```
- `BlockArm_SetAutoTarget()` 内 7 个 case 的角度赋值改为：
  `block_arm.dji_target_position = ARM_POSES[BLOCK_ARM_TARGET_XXX].dji_deg;`（zdrive 同理）
- 保留每个 case 原有的 `fine_adjust_profile` 赋值与 default 分支
- 顺手删除 7 处已填值却残留的 `/* TODO: 写入…目标 */` 注释（不影响行为）

### 步骤 6：修改 `IRQ/Src/CAN_IRQHandler.c`（合并双 handler，宏化信号）

- 头部加 `#include "arm_config.h"`
- 删除 `signal_type` 枚举（第 15-26 行，LEFT_/RIGHT_ 二义名字），case 改用 `ARM_*_SIGNAL` 宏
- 把 `Master_message_handler_left`（215-380 行）与 `right`（383-548 行）合并为一个 `Master_message_handler()`：两函数仅 4 个信号常量不同，其余 90% 相同 → 按左函数为基底，4 处常量替换为 `ARM_PICK_SIGNAL`/`ARM_PLACE_SIGNAL`/`ARM_RELEASE_SIGNAL`，另保留 0x00/0x01/0xFF/0xEE 分支一份
- 第 588-590 行回调调用点：
  ```c
  // 改前：
  // Master_message_handler_left(hcan, RxHeader, RxData);
  Master_message_handler_right(hcan, RxHeader, RxData);
  /*如果在右臂上烧录代码…*/
  // 改后：
  Master_message_handler(hcan, RxHeader, RxData);
  ```
- `Comm_RecordClick`、`Comm_ClickProcess` 不动

> 行为等价性：左 Target 编译后 `ARM_PICK_SIGNAL=0x05`，switch 只响应 0x05 组 = 现在 left 工程启用的 right handler；右 Target 只响应 0x03 组 = 现在 right 工程启用的 left handler。✓

### 步骤 7：`APL/Src/myostasks.c`（仅宏替换 FINISH 判定，行为不变）

| 位置 | 改前 | 改后 |
|---|---|---|
| 第 74 行 | `case 0x05U:` | `case ARM_PICK_SIGNAL:` |
| 第 78 行 | `case 0x06U:` | `case ARM_PLACE_SIGNAL:` |
| 第 81 行 | `case 0x08U:` | `case ARM_RELEASE_SIGNAL:` |
| 第 174/180/186 行 | `s_finish_process = 0x05U;` | `= ARM_PICK_SIGNAL;` |
| 第 191/196/201 行 | `= 0x06U;` | `= ARM_PLACE_SIGNAL;` |
| 第 206 行 | `= 0x08U;` | `= ARM_RELEASE_SIGNAL;` |

> 说明：这**不是修复**，只是把字面量换成宏，两个 Target 编译结果与现状完全一致（左板回 0x05/06/08，右板仍回 0x05/06/08 即现状不变）。不加 `#include` 外的任何其他改动，`flag`/`flag1~7` 等一概保留。

### 步骤 8：`MDK-ARM/f4_show.uvprojx`（双 Target）

- 复制整个 `<Target>...</Target>` 块（13 组 Groups 相对路径，无需改）
- Target1：`f4_show_LEFT`，`<Define>USE_HAL_DRIVER,STM32F405xx,ARM_LEFT</Define>`，输出 `left\f4_show_left`
- Target2：`f4_show_RIGHT`，`<Define>USE_HAL_DRIVER,STM32F405xx,ARM_RIGHT</Define>`，输出 `right\f4_show_right`
- 两 Target 的 `<IncludePath>` 追加 `..\Config`
- FML/新 Config 组中加入 `..\Config\arm_poses.c`

### 步骤 9：验证（二板实机冒烟，用户执行）

1. UV4 CLI 双 Target 编译：0 Error / 0 Warning
2. 对比 `.map`：left Target Code ≈ 原 LEFTNOW· 基线；right Target ≈ 原 RIGHTNOW 基线（均约 46764 ± 少量）
3. 实机：左板烧 f4_show_LEFT、右板烧 f4_show_RIGHT
   - 各自 Enable → 归零 → SAFE → 查询 0x00 正常
   - 三档取块 / 三档放块 + 释放 / Reset / Disable 全流程
   - **重点确认右板回执与改动前一致（现状回执码，不做"修正"）**
4. 全部通过 → `copy\RIGHTNOW` 暂**不删除**（等阶段二后再清），git 不操作

## 3. 行为等价性对照（验收依据）

| 项目 | 左固件 | 右固件 |
|---|---|---|
| 电磁阀通道 | 1 | 2 |
| DJI 电机下标 | 0 | 1 |
| 响应信号组 | 0x05/06/08 | 0x03/04/07 |
| 姿态角 | LEFTNOW· 实测值 | RIGHTNOW 实测值 |
| FINISH 回执 | 0x05/06/08 | 现状不变 |
| 死代码/未用模块 | 保留 | 保留 |
| 协议交叉 | 保持 | 保持 |

## 4. 风险与回退

- 双 Target XML 不顺 → 退化为双 uvprojx 共享源码（源码层不受影响）
- 实机行为与预期不符 → 全部差异集中在 `arm_config.h` + 姿态表，回滚只改这两个文件
- **本阶段绝不动：** MathFunc/pid/Vector/BlueTooth/ZDrive 的未用函数、flag 变量、协议逻辑